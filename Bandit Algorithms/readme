py files:
q_1_2.py contains the code for problem 1 and 2
q_3.py contains the code for problem 3
core.py imports all the required files
exp3.py for EXP3 algorithm
tests.py to run the algorithms

Run the following-

For question 1,
python3 -c 'from q_1_2 import *;q1()'

For question 2,
python3 -c 'from q_1_2 import *;q2()'

For question 3,

Part-1,
python3 -c 'from q_3 import *;q3_3()'  

Part-2,
python3 -c 'from q_3 import *;q3_3()'

Part-3,
python3 -c 'from q_3 import *;q3_3()'

Epsilon Greedy - standard one is used.
UCB - UCB1 is used.
EXP3 gamma - gamma=0.05 (No learning rate used in given code)
EXP3 - Learning rate used- 0.01

Due to low specification system horizon size used in 3(b) - 10^4,not 10^5.

